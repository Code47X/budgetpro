mutation ReorderBudgetItems($input: ReorderBudgetItemsInput!) {
  reorderBudgetItems(input: $input) {
    id
    type
    label
    budgetItems {
      id
      name
      position
      plannedAmount
    }
  }
}
