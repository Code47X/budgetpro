mutation RegisterUser($input: RegisterUserInput!) {
  register(input: $input) {
    ... on User {
      id
      firstName
      createdAt
    }
    ... on EmailAlreadyExists {
      message
    }
  }
}
